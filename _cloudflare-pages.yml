build:
  command: npm install && npx axcora build
  output: public
  environment:
    NODE_VERSION: "18"
    
routing:
  - source: "/assets/*"
    headers:
      Cache-Control: "public, max-age=31536000, immutable"
      
  - source: "/*"
    headers:
      X-Frame-Options: "DENY"
      X-Content-Type-Options: "nosniff"
